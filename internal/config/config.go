package config

// Avoid import cycle at all cost -so this config package will be imported by other packages but
//it wont import any other packages other then standard library packages .

type Appconfig struct {
}
